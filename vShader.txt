#version 460 core
layout (location = 0) in vec3 aPos;
layout (location = 1) in vec2 aTexCoord;

out vec3 ourColor;
out vec2 TexCoord;

uniform float sideM;

void main()
{
    gl_Position = vec4((aPos.x+sideM), aPos.y, aPos.z, 1.0);
    TexCoord = aTexCoord;
}