#version 460 core
out vec4 FragColor;

uniform int lightOrCrossHair;
uniform vec3 lightColor;

void main()
{
    if (lightOrCrossHair == 0)
        FragColor = vec4(1.0);
        FragColor = vec4(lightColor, 1.0);
    if (lightOrCrossHair == 1)
        FragColor = vec4(1.0);
}